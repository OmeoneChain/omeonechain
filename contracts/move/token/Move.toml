[package]
name = "omeone_token"
version = "0.1.0"
edition = "2024.beta"

[dependencies]
Sui = { git = "https://github.com/iotaledger/iota.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

[addresses]
omeone = "0x0"
omeonechain = "0x0"  # Keep for compatibility with your existing code
sui = "0x2"

[dev-addresses]
omeone = "0xCAFE"
omeonechain = "0xCAFE"
